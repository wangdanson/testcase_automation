# Gemini CLI 錯誤學習紀錄 (Error Log)

> **自動化守門員狀態**: ✅ **Active** (`validate_csv.py`)
> 此檔案紀錄歷史錯誤與修正邏輯，用於產出前的自我預檢 (Self-Correction)。

## 1. 🛑 [Critical] 系統邏輯與權限 (Logic & Permissions)

| 模組 | 規則描述 | 避坑指南 | 來源/案例 |
|---|---|---|---|
| **Identity** | **AOE 身分對等原則** | **Agency ID 571 專屬於 AOE 使用**。產出案例時應統一角色認知，所有歸屬於 ID 571 的操作即代表 AOE 權限行為。 | User Correction |
| **Style** | **嚴禁技術標籤外流** | **絕對禁止**在 CSV 的「測試功能」或「測試情境」中出現 `[七位一體]`、`原子化`、`整合式`、`Hybrid` 等 QA 技術標記。僅允許 `SKILL.md` 定義之全形標籤（如 `【正向】`）。 | **Repeated Error (Fixing)** |
| **Budget** | **超跑顯示與預算回收** | 1. 移除花費上限限制。2. `ABORTED` 狀態必須釋放剩餘預算。 | Pilot Phase 1 |
| **Identity** | **果實夥伴 (ID 571) 特權** | 1. 素材免審核。2. 產業類別必填且可見。3. 進階設定權限。 | Pilot Phase 2 |

## 2. ⚠️ [Major] 資料整合與 UI 連動

| 模組 | 規則描述 | 避坑指南 | 來源/案例 |
|---|---|---|---|
| **Logic** | **權限正負向分離原則** | 權限測試**必須拆分**為獨立的正向（開啟）與反向（屏蔽）案例，嚴禁寫在同一列。 | User Correction |
| **Style** | **文字純淨化** | 情境描述應改用「驗證...機制」、「檢查...完整性」，嚴禁使用「作為使用者...」模板。 | User Correction |
| **Strategy** | **雙標籤強制規範** | **必須**使用兩類標籤組成，格式為 `【分類】【性質】功能名稱`（如 `【權限】【正向】`），嚴禁只使用單一標籤。 | User Correction (Pilot Phase 2) |
| **Strategy** | **過濾瑣碎技術邏輯** | **禁止**將純技術欄位名稱映射（如 MIB -> MIC）列為測試項目。應專注於業務價值與 UI 變更之測試。 | User Correction (Pilot Phase 2) |
| **Strategy** | **人工驗證主清單優先** | 預設產出可人工執行的主清單，維持適中顆粒度；API payload、option API、欄位映射、低機率交叉情境應放到補充/回歸池，不得讓主清單造成驗證疲勞。範例檔只參考顆粒度與語氣，不得跨系統複製功能內容。 | User Correction (2026-05-11) |
| **Workflow** | **規格分析到上傳端到端執行** | 使用者要求分析規格文件並產出 test case 時，預設直接完成規格分析、CSV 產出、`validate_csv.py` 驗證與 `upload_to_sheets.py` 上傳；產出、驗證、上傳視為同一個全自動化任務，不得先停下詢問流程同意。只有使用者明確要求「不要上傳」或「只產 CSV」時，才不執行 Google Sheets 上傳；若明確要求先預覽或只分析，才停止在對應階段。 | User Correction (2026-05-12) |
| **Identity** | **ODM 審核查核角色** | 驗證 ODM 列表中 AOE 素材過濾之行為，測試角色應使用 **Onead User (AOE Admin)**，而非 PAD。 | User Correction (Pilot Phase 2) |
| **L10n** | **繁中語系標籤修正** | 確保姓氏在前、名字在後，對應 `lastName` 與 `firstName` | Pilot Phase 1 |

## 3. 🛡️ [Anti-Regression] 功能與邏輯回歸防護 (Regression Prevention)

| 模組 | 規則描述 | 避坑指南 | 來源/案例 |
|---|---|---|---|
| **Logic** | **防範指令競爭 (Instruction Competition)** | 產出時嚴禁因過度專注於「新格式規範」而忽略「基礎功能模組」（如黑白名單、時段）。 | **User Correction (2026-04-09)** |
| **Logic** | **功能表面積校驗 (Test Surface Area)** | 針對 Phase 系列升級，**必須參考前一版本**案例，確保歷史功能（黑白名單、受眾操作）在全角色隔離中依然存在。 | **Repeated Omission Fix** |

## 4. 🛡️ [Anti-Regression] 防回歸預檢清單


> **規則更新時之強制動作**: 任何對 `SKILL.md` 或 `GEMINI.md` 的結構化重組，必須保留以下「憲法級錨點」：

| 錨點名稱 | 規則描述 (不可刪除) | 避坑指南 |
|---|---|---|
| **ID 571 語法糖** | 涉及 ID 571 驗證，第一步必為：「以『果實夥伴』代理商權限之帳號登入系統」 | 嚴禁在結構重組時將此語法糖精簡或刪除。 |
| **雙標籤規範** | 測試功能必須使用 `【分類】【性質】` 雙標籤格式。分類應精確對應 `SKILL.md` 定義之維度（如 `【狀態】`、`【量測】`、`【回歸】`）。 | 確保分類與性質同時具備，提升過濾效率。 |
| **全包裹雙引號** | CSV 全欄位包裹雙引號 (") 且實體換行。 | 這是 `validate_csv.py` 的強制要求。 |
| **權限對稱覆蓋** | 當功能鎖定特定帳號時，反向隔離測試**必須分別針對** `superdsp_agency`、`superdsp_client`、`superdsp_media` 產出獨立案例。 | 嚴禁只用一個一般帳號代表所有外部權限，須預防權限外溢。 |
| **角色命名** | 內部使用 `Onead User (AOE)` 等規範；外部帳號必須**完整輸出技術名稱**（如 `superdsp_agency`）。 | 確保權限與系統底層定義完全對齊。 |

### 📅 2026-05-04 (測試案例格式與邏輯遺漏)
*   **錯誤**: 在產出 CSV 時使用 '\n' 字串而非實體換行。
*   **錯誤**:  欄位標籤格式不正確，缺少雙重標籤維度。
*   **錯誤**: 遺漏「ID 571」登入語法糖，未符合憲法級登入規範。
*   **修正**: 必須在 CSV 產出時使用 printf 或 heredoc 實現真實換行，並強制執行雙標籤與 571 登入步驟。

### 📅 2026-05-04 (權限矩陣遺漏)
*   **錯誤**: 僅產出 AOE/Partners 特權案例，遺漏了 Agency/Client/Media 的反向權限隔離驗證。
*   **修正**: 當功能涉及特定權限（如 AOE）時，必須原子化拆分出其他角色的「權限不可見/不可編輯」案例，確保符合全角色矩陣驗證規範。

### 📅 2026-05-04 (標籤幻覺與亂定義)
*   **錯誤**: 在修復驗證錯誤時自行發明標籤（如 【安全性】【隔離】），導致標籤系統散亂且失去業務語意。
*   **修正**: 標籤必須嚴格遵守以下白名單組合，嚴禁發明新詞：
    *   **第一維度 (分類)**: 【權限】、【功能】、【UI】、【數據】、【狀態】、【回歸】
    *   **第二維度 (性質)**: 【正向】、【反向】、【正向】、【反向】
*   **檢查點**: 產出前必須回溯參考 [終極嚴格矩陣版] 的標籤風格，確保 100% 擬人化對齊。

### 📅 2026-05-05 (測試功能命名冗詞)
*   **錯誤**: 在 `測試功能` 欄位的雙標籤後固定加上「驗證」，使名稱冗長且不夠自然。
*   **修正**: `測試功能` 欄位應保留 `【分類】【性質】` 雙標籤，但功能名稱本體直接描述操作或結果，不需再加上「驗證」前綴。
*   **範例**: 使用 `【權限】【正向】ID 571 素材包自動通過`，不要寫成 `【權限】【正向】驗證 ID 571 素材包自動通過`。

### 📅 2026-05-05 (測試功能命名過度情境化)
*   **錯誤**: `測試功能` 欄位將完整情境或預期結果濃縮成一句敘述，導致內容看起來像 `測試情境`，不夠像功能標題。
*   **修正**: `測試功能` 應寫成短標題，聚焦在功能點、限制條件或結果名稱，不重述角色、原因或完整流程。
*   **範例**: 使用 `【回歸】【正向】Sequence 保持舊媒體 URL`，不要寫成 `【回歸】【正向】未編輯活動不會更新既有 Sequence`。

### 📅 2026-05-05 (測試功能命名不夠專業)
*   **錯誤**: `測試功能` 欄位混入口語、描述句或不一致術語，導致測項名稱不夠專業。
*   **修正**: `測試功能` 應優先採用 QA 與需求文件常用名詞結構，例如 `權限隔離`、`顯示控制`、`鎖定控制`、`更新機制`、`格式一致性`、`顯示規則`。
*   **範例**: 使用 `【權限】【反向】一般代理商 ODM 審核權限隔離`，不要寫成 `【權限】【反向】一般代理商不可存取 ODM 審核列表`。

### 📅 2026-05-05 (測試情境語氣過硬)
*   **錯誤**: `測試情境` 欄位大量以「驗證...」起手，語氣過硬且過度像規格條文。
*   **修正**: `測試情境` 應採白話但具專業感的描述方式，可直接說明操作背景與預期行為，保留關鍵術語如 `審核流程`、`權限範圍`、`欄位控制`、`Sequence`、`URL 規則`。
*   **範例**: 使用 `果實夥伴代理商上傳素材包後，系統直接走自動通過流程`，不要寫成 `驗證果實夥伴代理商上傳素材包後可直接通過審核並立即投入後續流程`。

### 📅 2026-05-11 (人工驗證清單疲勞度)
*   **錯誤**: 為了追求覆蓋率，將 API 權限、option API、payload 欄位、Studio 回填、報表邊界等補充/回歸項目全部塞進同一份人工驗證 CSV，導致清單雖完整但人工執行疲勞感高。
*   **修正**: 往後任何系統或版本規格，預設先產「人工驗證主清單」。主清單以核心流程、權限邊界、關鍵 UI/UX、資料保存與跨系統狀態一致性為主；技術細節與低機率交叉案例只在使用者要求 expanded/regression/automation backlog 時另產。
*   **基準**: `SuperDSP_Pilot_for_AOE_(Phase_2)_test_case_20260511_110436.csv` 只作為顆粒度、語氣與人工可執行性的參考，不作為跨系統功能內容來源；`20260511_112403_expanded.csv` 類型應定位為補充/回歸池。
*   **防呆**: 不得因參考 AOE 範例而在其他系統產出 AOE、ID 571、素材免審核、OneAD 產業欄位等不屬於當次規格的測項。每一筆測項都必須能回扣到當次規格、同系統前版差異或 `spec_knowledge/` 既有規則。

### 📅 2026-05-11 (test case 產出流程授權)
*   **錯誤**: 使用者已明確要求「分析規格文件後產出 test case」時，仍可能停在功能地圖確認、產出同意或上傳同意，造成流程中斷。
*   **修正**: 往後遇到同類請求，預設直接執行完整流程：讀取規格、建立功能地圖、產出人工驗證主清單 CSV、執行 `validate_csv.py`、執行 `upload_to_sheets.py`。只有在使用者明確說先預覽、不要上傳、只分析，或工具權限/規格來源有問題時才停下詢問。
*   **邊界**: sandbox 網路權限或檔案權限要求仍需照執行環境規則請求授權，這不屬於流程同意問題。

### 📅 2026-05-12 (test case 全自動化流程不得等待流程同意)
*   **錯誤**: 即使 SOP 已定義端到端執行，仍可能把「產出 test case、驗證、上傳」拆成多個需要使用者同意的節點，導致使用者離開時任務卡在上傳前。
*   **修正**: 使用者要求產出 test case 時，預設即代表授權完整流程：產出 CSV、執行 `validate_csv.py`、執行 `upload_to_sheets.py`。不得再詢問是否要驗證或是否要上傳。
*   **例外指令**: 只有使用者明確說「不要上傳」或「只產 CSV」時，才不執行 Google Sheets 上傳；若明確要求「先預覽」或「只分析不產出」，才停止在對應階段。
*   **工具權限邊界**: 若 sandbox 或網路限制強制要求執行權限，應依環境規則請求並優先使用可持久化的 `python3 upload_to_sheets.py` prefix rule；這是工具層權限，不是流程同意。
